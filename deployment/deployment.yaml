apiVersion: v1
kind: Namespace
metadata:
  name: netflix-app

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: netflix-app-usage
  namespace: netflix-app
spec:
  selector:
    matchLabels:
      app: netflix-app
  template:
    metadata:
      labels:
        app: netflix-app
    spec:
      containers:
      - name: netflix-app
        image: prateekkumawat/jenkinsimage:netflixapp-rc-1.0
        resources:
          requests: 
            cpu: "500m"
            memory: "128Mi"
          limits:
            memory: "500Mi"
            cpu: "800m"
        ports:
        - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: netflix-app-svc
  namespace: netflix-app
spec:
  selector:
    app: netflix-app
  type: NodePort  
  ports:
  - port: 80
    targetPort: 80
