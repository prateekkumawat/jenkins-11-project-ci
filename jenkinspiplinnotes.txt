- setup git repo for code and jenkinsfile 
- seprate your jenkinsfile using build folder 
- jenkinsfile manage using git this method call "pipline-as-a code" that mean we use code based pipline. 
- we are using docker so install docker in your agent machine 
- setup your agent connnect user can run your docker commands
- usermod -aG docker username 
- **** if user join in current shell of user that not impact so close your shell terminal and check command running else reboot system.
***** refreance for jenkins pipline syntax ****
- https://www.jenkins.io/doc/book/pipeline/syntax/#agent